// ---------------------------------------------------------------- //
// This file is autogenerated by pioasm version 2.2.0; do not edit! //
// ---------------------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// --------- //
// swd_write //
// --------- //

#define swd_write_wrap_target 0
#define swd_write_wrap 1
#define swd_write_pio_version 0

static const uint16_t swd_write_program_instructions[] = {
            //     .wrap_target
    0x6001, //  0: out    pins, 1         side 0
    0xb042, //  1: nop                    side 1
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program swd_write_program = {
    .instructions = swd_write_program_instructions,
    .length = 2,
    .origin = -1,
    .pio_version = swd_write_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config swd_write_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + swd_write_wrap_target, offset + swd_write_wrap);
    sm_config_set_sideset(&c, 1, false, false);
    return c;
}
#endif

// -------- //
// swd_read //
// -------- //

#define swd_read_wrap_target 0
#define swd_read_wrap 1
#define swd_read_pio_version 0

static const uint16_t swd_read_program_instructions[] = {
            //     .wrap_target
    0xa042, //  0: nop                    side 0
    0x5001, //  1: in     pins, 1         side 1
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program swd_read_program = {
    .instructions = swd_read_program_instructions,
    .length = 2,
    .origin = -1,
    .pio_version = swd_read_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config swd_read_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + swd_read_wrap_target, offset + swd_read_wrap);
    sm_config_set_sideset(&c, 1, false, false);
    return c;
}

#include "hardware/clocks.h"
static inline void swd_write_program_init(PIO pio, uint sm, uint offset,
                                           uint pin_swdio, uint pin_swclk,
                                           float freq) {
    pio_sm_config c = swd_write_program_get_default_config(offset);
    /* OUT = SWDIO */
    sm_config_set_out_pins(&c, pin_swdio, 1);
    pio_gpio_init(pio, pin_swdio);
    pio_sm_set_consecutive_pindirs(pio, sm, pin_swdio, 1, true);
    /* Sideset = SWCLK */
    sm_config_set_sideset_pins(&c, pin_swclk);
    pio_gpio_init(pio, pin_swclk);
    pio_sm_set_consecutive_pindirs(pio, sm, pin_swclk, 1, true);
    sm_config_set_out_shift(&c, true, true, 32);
    float div = clock_get_hz(clk_sys) / (freq * 2.0f);
    sm_config_set_clkdiv(&c, div);
    pio_sm_init(pio, sm, offset, &c);
    pio_sm_set_enabled(pio, sm, true);
}
static inline void swd_read_program_init(PIO pio, uint sm, uint offset,
                                          uint pin_swdio, uint pin_swclk,
                                          float freq) {
    pio_sm_config c = swd_read_program_get_default_config(offset);
    /* IN = SWDIO */
    sm_config_set_in_pins(&c, pin_swdio);
    pio_gpio_init(pio, pin_swdio);
    pio_sm_set_consecutive_pindirs(pio, sm, pin_swdio, 1, false);
    /* Sideset = SWCLK */
    sm_config_set_sideset_pins(&c, pin_swclk);
    pio_gpio_init(pio, pin_swclk);
    pio_sm_set_consecutive_pindirs(pio, sm, pin_swclk, 1, true);
    sm_config_set_in_shift(&c, true, true, 32);
    float div = clock_get_hz(clk_sys) / (freq * 2.0f);
    sm_config_set_clkdiv(&c, div);
    pio_sm_init(pio, sm, offset, &c);
    pio_sm_set_enabled(pio, sm, true);
}

#endif

